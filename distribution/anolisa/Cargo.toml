[workspace]
# MSRV-aware resolution: dependency versions are held back to what
# `rust-version` below can still compile. RPM source builds use the
# distro's rustc, so a dependency release that outruns the MSRV must not
# be picked up silently by `cargo update`.
resolver = "3"
members = [
    "crates/anolisa-cli",
    "crates/anolisa-core",
    "crates/anolisa-env",
    "crates/anolisa-build",
    "crates/anolisa-platform",
]

[workspace.package]
version = "0.3.16"
edition = "2024"
license = "Apache-2.0"
rust-version = "1.93"
publish = false

[workspace.dependencies]
# CLI
clap = { version = "4", features = ["derive", "env", "color"] }

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml_ng = "0.10"
toml = "0.8"
# AST-level TOML editing — used by sandbox_install.rs to patch
# /etc/containerd/config.toml without losing user comments / ordering and
# without producing duplicate plugin tables (containerd 2.x is strict).
toml_edit = "0.22"

# Error handling
thiserror = "2.0"
anyhow = "1.0"

# Terminal UI
console = "0.15"

# Data structures
semver = "1.0"
chrono = { version = "0.4", default-features = false, features = [
    "std",
    "clock",
    "serde",
] }

# Linux / System
dirs = "6"
nix = { version = "0.29", default-features = false, features = ["user", "fs", "socket", "signal"] }

# File integrity
sha2 = "0.10"
sha1 = "0.10"
md-5 = "0.10"
url = "2"
percent-encoding = "2"
xattr = "1.6"

# HTTP download
ureq = "2.12"
rustls-native-certs = "0.8"
rust-ini = "0.21"
env_proxy = "0.4"
base64 = "0.22"

rpmrepo_metadata = { version = "0.7.0", default-features = false }
quick-xml = { version = "0.42", default-features = false }
# Keep metadata decoding independent of the target host's liblzma version.
liblzma = { version = "0.4", default-features = false, features = ["static"] }

# Archive handling
flate2 = "1"
tar = "0.4"

# File locking
fs2 = "0.4"

# Unique identifiers
uuid = { version = "1", default-features = false, features = ["v4", "serde"] }

# Test utilities
tempfile = "3"

# Internal crates
anolisa-core = { path = "crates/anolisa-core" }
anolisa-env = { path = "crates/anolisa-env" }
anolisa-build = { path = "crates/anolisa-build" }
anolisa-platform = { path = "crates/anolisa-platform" }

[profile.release]
lto = "thin"
codegen-units = 1
panic = "abort"
debug = true
strip = "debuginfo"
split-debuginfo = "packed"
